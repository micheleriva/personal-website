---
import { format } from 'date-fns'

import Layout from '../layouts/Layout.astro'
import Content from '../components/Content.astro'

import talks from '../../public/data/talks.json'

const emojiMap = {
  'usa': '🇺🇸',
  'italy': '🇮🇹',
  'germany': '🇩🇪',
  'united kingdom': '🇬🇧',
  'netherlands': '🇳🇱',
  'spain': '🇪🇸',
  'pakistan': '🇵🇰',
  'sweden': '🇸🇪',
  'norway': '🇳🇴',
  'ireland': '🇮🇪',
  'nigeria': '🇳🇬',
  'greece': '🇬🇷',
  'belgium': '🇧🇪',
  'france': '🇫🇷',
  'poland': '🇵🇱',
  'chile': '🇨🇱',
}
---

<Layout title="Public Speaking | Michele Riva" cover="michele-riva-talks.jpg">
  <Content title="Public Speaking">
    <div>
      <p class='my-4'>
        During my career as a software engineer, I've been incredibly fortunate to speak at numerous international conferences across more than 17 countries.
      </p>

      <p class='my-4'>
        If you find any of these talks interesting or would like me to participate in a conference or podcast episode, please don't hesitate to contact me—it's always a pleasure.
      </p>
    </div>

    <div class='mt-8'>
      <div>
        {talks.reverse().map((talk) => (
          <div class="border border-neutral-700 p-1.5 rounded-lg bg-neutral-950 bg-opacity-15 no-underline mb-2">
            <p class='font-bold'> {talk.title} </p>
            <div class='text-neutral-400 text-xs'>
              {format(talk.date, 'dd MMM yyyy')} - {talk.event} - {talk.city}, {talk.country} <span>{emojiMap[talk.country.toLowerCase()]}</span>
            </div>

            {talk.url ? (
              <a href={talk.url} class='text-blue-400 hover:text-blue-400 text-xs'> Watch recording</a>
            ) : (
              <span class='text-neutral-600 text-xs'> Recording not available </span>
            )}

          </div>
        ))}
      </div>
    </div>
  </Content>

</Layout>